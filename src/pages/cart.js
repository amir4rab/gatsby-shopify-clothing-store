import React from 'react';
import Layout from '../layout/layout.component';

const CartPage = () => {
    return (
        <Layout>
            <div>
                Cart
            </div>
        </Layout>
    );
};

export default CartPage;
