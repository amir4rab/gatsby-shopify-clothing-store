import React from 'react';
import Layout from '../layout/layout.component';

const AccountPage = () => {
    return (
        <Layout>
            <div>
                Account
            </div>
        </Layout>
    );
};

export default AccountPage;
